@model PagedList.IPagedList<LMS.Models.Book>
@using PagedList.Mvc

@{
    ViewBag.Title = "Old Books";
}

<div class="row">
    <div class="col-md-12">
        <div class="box">
            <div class="box-body">
                <table class="table table-bordered">
					<thead>
						<tr>
							<th>
								Book ID
							</th>
							<th>
							    Book Name
							</th>
							<th>
								Book Copy
							</th>
							<th>
								Location
							</th>		
						</tr>
					</thead>
					<tbody>

						@foreach (var item in Model) {
							<tr>
								<td>

								</td>
								<td>

								</td>
								<td>

								</td>
                                <td>

                                </td>
							</tr>
						}
					</tbody>
				</table>
			</div>
			<div class="box-footer clearfix">
                <div class="pull-right">
                    <a class="btn btn-danger pull-right" href="@Url.Action("Delete")"><i class="fa fa-trash"></i> Delete</a>
                </div>
                <div class="pull-right">
                    @Html.PagedListPager(Model, page => Url.Action("OldBooks", new { page }))
                </div>
            </div>
		</div>
	</div>
</div>
